import React from 'react'
import VideoCategory from '../../../../reusable/VideoCategory/VideoCategory'

const History = () => {
    return (
        <VideoCategory category={"History"} firstIndex={0} lastIndex={15} />
    )
}

export default History